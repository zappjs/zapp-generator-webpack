default:
  app:
    - ./Main.jsx
  host: localhost
  port: 3000
development:
  app:
    - 'webpack-dev-server/client?http://${host}:${port}'
    - ./Main.jsx
  outputPath: tmp
production:
  outputPath: build
